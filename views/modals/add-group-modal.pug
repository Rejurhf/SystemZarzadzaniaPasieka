div(id="modalAddGroup" class="modal")
    .modal-content(action="/group" action-type="POST" grid-refresh="True")
        .modal-header
            span(class="close" onclick="closeModal(this)") &times;
            h2 Dodaj Grupę
        .modal-body
            form
                .inputBox.apiaryID
                    label Pasieka: 
                    select(not-null="true" name="apiaryID")
                        option(value="") 
                .inputBox.groupName
                    label Nazwa grupy: 
                    input(type="text" not-null="true" name="groupName")
                .inputBox.creationDate
                    label Data stworzenia:
                    input(type="date" not-null="true" 
                        name="creationDate") 
        hr
        .modal-footer
            button.secondary(onclick="closeModal(this)") Anuluj
            button(onclick="validateModal(this)") Dodaj
